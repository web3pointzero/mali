                <div class="inside">
                	<h2>Makaleler</h2><hr/>
                	{% for makale in makale_list %}
                  <div class="wrapper">
                    <h2><a href="/makaleler/{{ makale.id }}" class="link1">{{ makale.title }}</a></h2>
                    <!-- <img src="/static/images/1page-img1.jpg" alt="" class="img-indent"> -->
                    <p>{{ makale.content|truncatewords_html:50|safe }}</p>
                    <a href="/makaleler/{{ makale.id }}">Makalenin devamı için tıklayınız.</a>
                  </div><hr/>
                  {% endfor %}
                  <div class="pagination">
                    <span class="step-links">
                        {% if makale_list.has_previous %}
                            <a href="?page={{ makale_list.previous_page_number }}">previous</a>
                        {% endif %}

                        <span class="current">
                            Page {{ makale_list.number }} of {{ makale_list.paginator.num_pages }}.
                        </span>

                        {% if makale_list.has_next %}
                            <a href="?page={{ makale_list.next_page_number }}">next</a>
                        {% endif %}
                    </span>
                </div>
                </div>

